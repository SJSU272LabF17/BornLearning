<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColorWhite</item>
        <item name="android:editTextColor">@color/textColorWhite</item>
        <item name="android:textColorPrimary">@color/textColorWhite</item>
        <item name="android:textColorSecondary">@color/textColorWhite</item>
        <item name="android:textColorTertiary">@color/textColorWhite</item>
        <item name="android:textColorPrimaryInverse">@color/textColorWhite</item>
        <item name="android:textColorSecondaryInverse">@color/textColorWhite</item>
        <item name="android:textColorTertiaryInverse">@color/textColorWhite</item>
    </style>

</resources>
